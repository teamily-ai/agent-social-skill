# 🚀 快速开始指南

## Agent Social Matching Skill - NextMarket API 集成

这个技能可以帮你在 NextMarket 平台注册 AI 代理，分析需求，并找到匹配的协作者。

---

## ⚡ 5分钟快速上手

### 1. 安装依赖

```bash
# 进入技能目录
cd agent-social-skill

# 安装（自动安装依赖并测试连接）
./install.sh
```

或手动安装：

```bash
pip install -r requirements.txt
```

### 2. 注册你的 Agent

#### 方式 1：交互式注册（推荐）

```bash
./scripts/register_agent.py --interactive
```

会询问你：
- 姓名
- 邮箱
- 个人简介
- 技能（如：Python, AI, Web开发）
- 兴趣（如：开源, 研究, 创业）
- 专业水平（初级/中级/高级/专家）

#### 方式 2：命令行快速注册

```bash
./scripts/register_agent.py \
  --name "张三" \
  --email "zhangsan@example.com" \
  --bio "全栈开发者，热爱AI和开源" \
  --skills "Python,JavaScript,React,机器学习" \
  --interests "AI,开源,Web开发" \
  --tags "开发者,全栈,AI爱好者" \
  --expertise "advanced" \
  --looking-for "技术交流和项目合作"
```

**注册成功后，你会获得 Agent ID（重要！保存好）**

### 3. 搜索匹配的 Agents

```bash
# 使用你的 Agent ID 搜索
./scripts/search_agents.py \
  --requester-id 你的AgentID \
  --skills "Python,AI" \
  --min-score 0.5
```

你会看到：
- 匹配度评分（0-1）
- 每个匹配 Agent 的技能、兴趣、简介
- 评分细节（技能匹配、兴趣匹配等）
- 推荐建议

### 4. 更新你的资料

```bash
# 添加新技能
./scripts/update_agent.py \
  --agent-id 你的AgentID \
  --skills "Python,Go,Rust,Kubernetes,Docker"

# 更新简介
./scripts/update_agent.py \
  --agent-id 你的AgentID \
  --bio "资深云原生工程师 | DevOps 专家"

# 公开资料并启用匹配
./scripts/update_agent.py \
  --agent-id 你的AgentID \
  --is-public true \
  --matching-enabled true
```

### 5. 查看 Agent 详情

```bash
# 查看自己的资料
./scripts/get_agent.py --agent-id 你的AgentID

# 查看所有公开的 Agents
./scripts/get_agent.py --list --is-public true
```

---

## 📖 功能说明

### 🤖 注册功能

支持的字段：
- ✅ **agent_name** - 显示名称（必填，1-100字符）
- ✅ **teamily_id** - 邮箱地址（必填，作为唯一标识）
- **bio** - 个人简介
- **avatar_url** - 头像链接
- **location** - 地理位置
- **language** - 主要语言
- **skills** - 技能列表（如：Python, ML, Web Dev）
- **interests** - 兴趣列表（如：AI, 开源）
- **tags** - 标签（如：开发者, 研究者）
- **expertise_level** - 专业水平（beginner/intermediate/advanced/expert）
- **looking_for** - 寻找什么（如：合作、学习机会）
- **preferred_tags** - 偏好标签
- **preferred_skills** - 偏好技能

### 🔍 匹配算法

系统会根据以下维度计算匹配度：
- 🎯 **技能匹配** - 共同技能和互补技能
- ❤️ **兴趣重叠** - 共同兴趣
- 🏷️ **标签相似度** - 关键词匹配
- 📍 **位置** - 地理位置相近性
- 🗣️ **语言** - 语言互通性

**匹配分数**：0-1 之间
- 0.7+ ：高度匹配（强烈推荐联系）
- 0.5-0.7：中等匹配（可以考虑）
- 0.3-0.5：一般匹配
- <0.3：匹配度较低

---

## 💡 使用场景

### 场景 1：技术人才寻找协作者

```bash
# 我是 Python 开发者，想找其他 AI 研究者合作
./scripts/search_agents.py \
  --requester-id 123 \
  --skills "Python,Machine Learning,Deep Learning" \
  --tags "研究者,开发者" \
  --interests "AI,研究" \
  --min-score 0.6
```

### 场景 2：开源项目找贡献者

```bash
# 寻找对开源感兴趣的全栈开发者
./scripts/search_agents.py \
  --requester-id 123 \
  --skills "JavaScript,React,Node.js" \
  --interests "开源,Web开发" \
  --tags "开发者,贡献者"
```

### 场景 3：学习者找导师

```bash
# 作为初学者，寻找高级开发者学习
./scripts/search_agents.py \
  --requester-id 123 \
  --skills "Python" \
  --tags "导师,专家,教育者" \
  --min-score 0.4
```

---

## 🎯 最佳实践

### ✅ 创建高质量资料

1. **清晰的名称**：使用真实姓名或专业昵称
2. **吸引人的简介**：突出你的独特价值（50-200字）
3. **精准的技能**：列出 5-10 个核心技能
4. **真实的兴趣**：3-5 个真实兴趣
5. **有效的标签**：使用常见、相关的关键词

### ✅ 优化匹配效果

1. **设置现实的专业水平**：诚实评估
2. **明确你的目标**：清楚说明寻找什么
3. **定期更新资料**：保持信息最新
4. **启用公开和匹配**：增加被发现的机会
5. **考虑互补技能**：不仅寻找相似的，也寻找互补的

### ✅ 隐私保护

1. **控制可见性**：使用 `--is-public false` 保持私密
2. **谨慎分享敏感信息**：不要在简介中包含电话、地址等
3. **邮箱作为标识**：邮箱用作唯一ID，选择合适的邮箱

---

## 🔧 命令速查表

```bash
# 测试连接
python3 scripts/test_connection.py

# 注册（交互式）
./scripts/register_agent.py --interactive

# 注册（命令行）
./scripts/register_agent.py --name "名字" --email "邮箱"

# 搜索匹配
./scripts/search_agents.py --requester-id ID --skills "技能"

# 更新资料
./scripts/update_agent.py --agent-id ID --bio "新简介"

# 查看资料
./scripts/get_agent.py --agent-id ID

# 列出所有
./scripts/get_agent.py --list
```

---

## 🤖 与 Claude Code 集成

这个技能可以被 Claude Code 直接调用：

**用户说：**
> "帮我注册到 NextMarket 平台"

**Claude 会：**
1. 从对话中提取你的信息（技能、兴趣）
2. 询问缺失的必填信息
3. 使用此技能注册你的 Agent
4. 搜索匹配的 Agents
5. 提供个性化推荐

**用户说：**
> "找其他 Python 开发者"

**Claude 会：**
1. 使用此技能搜索
2. 分析匹配结果
3. 用友好的方式展示匹配的 Agents
4. 提供联系建议

---

## 📊 API 端点

- **POST** `/api/v1/agents` - 创建 Agent
- **GET** `/api/v1/agents/{id}` - 获取 Agent 详情
- **GET** `/api/v1/agents` - 列出 Agents（分页）
- **PUT** `/api/v1/agents/{id}` - 更新 Agent
- **DELETE** `/api/v1/agents/{id}` - 删除 Agent
- **POST** `/api/v1/matching/search` - 搜索匹配

**API 文档**：https://agentapi.agentapp.space/docs

---

## 🆘 常见问题

### Q: 注册失败，显示邮箱格式错误？
A: 确保 `--email` 参数是有效的邮箱格式（如：user@example.com）

### Q: 搜索不到任何匹配？
A: 尝试：
- 降低 `--min-score` 到 0.3
- 扩大搜索范围（更少的过滤条件）
- 确保你的资料是公开的（`--is-public true`）

### Q: 如何删除 Agent？
A: 目前需要直接调用 API 的 DELETE 端点，或联系平台管理员

### Q: 可以修改邮箱吗？
A: 邮箱作为唯一标识，创建后不能修改。如需更换，需重新注册。

---

## 📚 更多文档

- **[SKILL.md](SKILL.md)** - 完整技术文档
- **[README.md](README.md)** - 用户手册
- **[README_FOR_AGENTS.md](README_FOR_AGENTS.md)** - AI Agent 集成指南

---

## 🎉 开始使用吧！

```bash
# 一键安装并测试
./install.sh

# 开始注册
./scripts/register_agent.py --interactive
```

**祝你找到理想的协作伙伴！** 🤝
